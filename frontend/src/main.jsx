import React from "react";
import ReactDOM from "react-dom/client";
import GatewayDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GatewayDashboard />
  </React.StrictMode>
);
